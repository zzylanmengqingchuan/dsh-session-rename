{
  "name": "dsh-session-rename",
  "version": "0.1.0",
  "description": "Custom session titles for the dsh web GUI: click the conversation header title (or the pencil beside it) to rename the current session; the user-set title syncs to the sidebar history list and persists, and pins the session against automatic re-titling. Never touches session type, modes (standard/Think/Bash), model, or agent behavior.",
  "type": "module",
  "main": "./lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./client": "./lib/client.js",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "cordis.patch.yml",
    "README.md",
    "README.zh.md"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "inject": [
        "@deepseek-ai/dsh-client-runtime",
        "@deepseek-ai/dsh-client-locale"
      ],
      "platform": "web"
    }
  },
  "license": "MIT",
  "peerDependencies": {
    "react": "^18.2.0"
  }
}
